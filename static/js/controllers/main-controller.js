(function (ng) {
  'use strict';

  ng.module('app.controllers').controller('MainController', [
    '$scope', '$http', '$q', '$location',

    function ($scope, $http, $q, $location) {
      // MainController
    }
  ]);
})(angular);
